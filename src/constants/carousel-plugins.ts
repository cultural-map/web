import AutoScroll from "embla-carousel-auto-scroll";

export const AUTO_SCROLL_PLUGIN = AutoScroll({
	speed: 0.5,
	startDelay: 0,
	playOnInit: true,
	stopOnFocusIn: true,
	stopOnMouseEnter: true,
	stopOnInteraction: false,
});
